{
  "name": "orbitaskflow",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "echo 'Running all tests...' && pnpm run test:js && pnpm run test:go && pnpm run test:py",
    "test:js": "echo 'Running JS/TS tests...' && pnpm dlx turbo run test",
    "test:go": "echo 'Running Go tests...' && (cd services/site-auth && go test ./...)",
    "test:py": "echo 'Running Python tests...' && (cd services/agent-bridge && .venv/bin/pytest)",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "ci": "pnpm run test && pnpm run bulid"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@types/bcrypt": "^6.0.0",
    "@types/pg": "^8.15.6",
    "bcrypt": "^6.0.0",
    "depcheck": "^1.4.7",
    "husky": "^9.1.6",
    "pg": "^8.16.3",
    "tsx": "^4.19.0",
    "turbo": "^2.1.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
